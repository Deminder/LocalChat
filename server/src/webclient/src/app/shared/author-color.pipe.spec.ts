import { AuthorColorPipe } from './author-color.pipe';

describe('AuthorColorPipe', () => {
  it('create an instance', () => {
    const pipe = new AuthorColorPipe();
    expect(pipe).toBeTruthy();
  });
});
