import { EnterKeyDownDirective } from './enter-key-down.directive';

describe('EnterKeyDownDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterKeyDownDirective();
    expect(directive).toBeTruthy();
  });
});
