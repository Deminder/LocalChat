import { FielderrorPipe } from './fielderror.pipe';

describe('FielderrorPipe', () => {
  it('create an instance', () => {
    const pipe = new FielderrorPipe();
    expect(pipe).toBeTruthy();
  });
});
